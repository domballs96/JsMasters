function ap(what_elm){const body = document.body; body.append(what_elm)} // appends child to body
function apc(what_elm){const body = document.body; body.appendChild(what_elm)} // appends child to body
// if you read it it allredy makes the body for the DOM 
// it really just makes "const body = document.body;append("new_div")" to ap() or "const body = document.body;appendChild("new_div")" to apc()
